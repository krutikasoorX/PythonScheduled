pipeline {
  agent any
  triggers {
        cron('*/5 * * * *')
    }
  environment {
        Num1 = 9
        Num2=80
    }
  stages {
    stage('version') {
      steps {
        bat 'py -3 --version'
      }
    }
    stage('hello') {
      steps {
        bat 'py -3 loopExec.py ${Num1} ${Num2}'
      }
    }
  }
}
