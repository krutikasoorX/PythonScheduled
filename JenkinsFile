pipeline {
  agent any
  triggers {
        cron('*/5 * * * *')
    }
  stages {
    stage('version') {
      steps {
        sh 'python3 --version'
      }
    }
    stage('hello') {
      steps {
        sh 'python3 loopExec.py'
      }
    }
  }
}
