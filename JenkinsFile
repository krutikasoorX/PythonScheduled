pipeline {
  agent any
  triggers {
        cron('*/5 * * * *')
    }
  environment {
        Num1 = 9
        Num2=80
    }
  stages {
    stage('version') {
      steps {
        bat 'python --version'
      }
    }
    stage('hello') {
      steps {
        bat 'python loopExec.py ${Num1} ${Num2}'
      }
    }
  }
}
